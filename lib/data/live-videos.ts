import type { LiveVideo } from "@/lib/types";

// No live performance videos supplied yet — add real YouTube links here (or
// via Sanity once connected) rather than embedding placeholder/stock video.
export const liveVideos: LiveVideo[] = [];
