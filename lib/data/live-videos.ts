import type { LiveVideo } from "@/lib/types";

// No individually-curated live performance videos yet — add entries here
// (or via Sanity once connected) for specific highlighted clips.
export const liveVideos: LiveVideo[] = [];

// Full YouTube playlist of live performances, embedded natively on /live.
export const livePlaylist = {
  id: "PLFRYu8FqywGE8NmM9vJ-KFp5ChVKBP8mk",
  title: "Live Performances",
};
