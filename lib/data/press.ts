import type { PressMention } from "@/lib/types";

// Populate with real quotes/URLs via Sanity once supplied. The BBC News
// feature is a known achievement (see lib/data/artist.ts) but no article
// URL has been provided yet, so it is intentionally omitted here.
export const pressMentions: PressMention[] = [];
