import type { Post } from "@/lib/types";

// Placeholder until posts are written in Sanity, matching the pattern used by
// the other content types. Empty rather than invented: the news index renders
// an honest empty state instead of fabricated announcements.
export const posts: Post[] = [];
